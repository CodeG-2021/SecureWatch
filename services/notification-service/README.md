# Notification Service

Service for internal alerts.

Responsibilities:

- Create notifications when a finding is `high` or `critical`.
- Mark notifications as read.
- Emit events to the real-time dashboard.
- Register events in the audit trail.
