# Apps

User-facing applications.

## Workspaces

- `web`: main frontend for administrators, analysts, and supervisors.

Convention: each app should own its build, dependencies, and tests, while consuming shared contracts from `packages/contracts` when available.
